name: 'cripto_dbt'
version: '1.0.0'
config-version: 2

profile: 'cripto_dbt'

model-paths: ["models"]

models:
  cripto_dbt:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
